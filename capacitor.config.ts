import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ecomerce.svelte.cpm',
  appName: 'Ecommerce',
  webDir: 'build',
  server: {}
};


export default config;
